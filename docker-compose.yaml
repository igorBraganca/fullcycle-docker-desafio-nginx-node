version: '3'

services:
  db:
    image: mysql
    container_name: db
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=123456
    networks:
      - node_network
    volumes:
      - ./mysql:/var/lib/mysql
  app:
    image: fullcycle/nodeapp
    build:
      context: ./app
    container_name: app
    restart: always
    environment:
      - DB_HOST=db
      - DB_PORT=3306
      - DB_USER=root
      - DB_PASSWORD=123456
      - DB_NAME=nodeDB
    networks:
      - node_network
    depends_on:
      - db
  nginx:
    image: fullcycle/nginx_nodeapp
    build:
      context: ./nginx
    container_name: nginx_app
    restart: always
    ports:
      - 8080:80
    networks:
      - node_network
    depends_on:
      - app

networks:
  node_network:
    driver: bridge
